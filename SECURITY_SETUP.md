# تشغيل نظام الدخول الآمن

تتطلب الواجهة الجديدة جلسة Supabase صالحة قبل عرض أي عميل أو رصيد أو حركة مالية. يسمح Supabase بشكل طبيعي بتسجيل دخول الحساب نفسه من أكثر من جهاز في الوقت نفسه؛ التطبيق لا ينفذ تسجيل خروج عام أو قيد جهاز واحد.

## قبل النشر

1. نفّذ ملف الترحيل `supabase/migrations/20260917000002_secure_auth_and_financial_posting.sql` من Supabase SQL Editor أو عبر Supabase CLI في بيئة الاختبار أولاً.
2. في Supabase Authentication، فعّل مزود البريد الإلكتروني واضبط روابط التحويل للموقع المنشور وللتطوير المحلي.
3. عطّل التسجيل الذاتي العام. أنشئ حسابات الموظفين من لوحة Supabase أو من مسار إداري يستخدم مفتاح الخدمة على الخادم فقط؛ لا يوضع مفتاح الخدمة في Vite أو Cloudflare Pages.
4. قبل إنشاء أي مستخدم، تأكد من وجود المنظمة والفرع المطلوبين. يستلزم trigger قاعدة البيانات حقول metadata التالية عند الإنشاء:

```json
{
  "organization_id": "00000000-0000-0000-0000-000000000001",
  "branch_id": "<optional branch UUID>",
  "full_name": "اسم الموظف",
  "phone": "01xxxxxxxxx",
  "role": "admin | manager | cashier | collector | sales | reports"
}
```

5. اختبر بحساب `cashier` وحساب `reports`: الأول يستطيع تسجيل التحصيل، والثاني يستطيع القراءة فقط. اختبر ذلك في جهازين مختلفين للحساب نفسه؛ كلا الجلستين يجب أن تبقيا نشطتين.

## قواعد مالية مهمة

- لا تُنشر هذه النسخة قبل تنفيذ الترحيل؛ بدونه ستظل سياسات الوصول السابقة تسمح بالوصول العام.
- كل تحصيل يمر بدالة قاعدة البيانات الذرية. عند الرفض أو فشل الشبكة لا يظهر إيصال ولا تتغير الواجهة إلى حالة نجاح.
- مباشرة بعد الترحيل، لا تمنح صلاحيات إدخال مباشرة إلى جداول الحركات المالية. استخدم دوال RPC المتحققة فقط.
- أنشئ مستخدم المدير الأول من لوحة Supabase مع metadata الصحيحة، ثم اختبر دخوله قبل إغلاق النافذة أو المتصفح المستخدم لإدارة المشروع.
